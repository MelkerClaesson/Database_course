Analyze and optimize the performance of these queries.
Identify and choose at least one SQL query which can run faster by
adding an index. Write SQL statement(s) for adding this index
(these indices). In you report, include the output of the EXPLAIN
command before and after adding the index (indices) as well.
